#define MACRO_TAP_TERM 10
#define MACRO_WAIT_TERM 10

#define MACRO(NAME, WAIT, TAP, BINDINGS) \
    NAME: NAME { \
        compatible = "zmk,behavior-macro"; \
        #binding-cells = <0>; \
        wait-ms = <WAIT>; \
        tap-ms = <TAP>; \
        bindings = <BINDINGS>; \
};

MACRO(dot_spc, MACRO_TAP_TREM, MACRO_WAIT_TERM, &kp DOT &kp SPACE &sk LSHFT)
